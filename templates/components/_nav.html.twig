

{% trans_default_domain 'navigation' %}
<!--menu mobile-->
<nav class="menu-res hidden-lg hidden-md ">
    <div class="menu-res-inner">
        <ul>
            <li>
                <a href="{{ path('index') }}">{{ 'nav.home' | trans }} </a>
            </li>
            {% for categorie in categories %}
                <li>
                    <a href="{{ path('index_categorie', {
                        slug: categorie.slug
                    }) }}">{{ categorie.nom }}</a>
                </li>
            {% endfor %}
        </ul>
    </div>
</nav>
<div class="page">
    <div class="container">
        <!--header-->
        <header class="header">
            <div class="row">
                <div class="col-md-3 col-sm-4 col-xs-12">
                    <a href="{{ path('index') }}" class="logo">
                        <img alt="Logo" src="{{ asset('images/logo.png') }}" />
                    </a>
                </div>
            </div>
        </header>
        <!--menu-->
        <nav class="menu font-heading">
            <div class="menu-icon hidden-lg hidden-md">
                <i class="fa fa-navicon"></i>
                <span>MENU</span>
            </div>
            <ul class="hidden-sm hidden-xs">
                <li {% if active == 'index' %}class="current"{% endif %}>
                    <a href="{{ path('index') }}">{{ 'nav.home' | trans }} </a>
                </li>
                {% for categorie in categories %}
                    <li {{ active == categorie.slug ? 'class="current"' }}>
                        <a href="{{ path('index_categorie', {
                            slug: categorie.slug
                        }) }}">{{ categorie.nom }}</a>
                    </li>
                {% endfor %}
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li>
                        <a href="{{ path('security_deconnexion') }}">{{ 'nav.logout' | trans }}</a>
                    </li>
                {% else %}
                    <li {{ active == 'connexion' ? 'class="current"' }}>
                        <a href="{{ path('security_connexion') }}">{{ 'nav.login' | trans }}</a>
                    </li>
                    <li {{ active == 'inscription' ? 'class="current"' }}>
                        <a href="{{ path('membre_inscription') }}">{{ 'nav.register' | trans }}</a>
                    </li>
                {% endif %}
            </ul>
            <div class="search-icon">
                <div class="search-icon-inner">
                    <i class="fa fa-search"></i>
                </div>
                <div class="search-box">
                    <input type="text" placeholder="{{ 'nav.search.placeholder' | trans }}" />
                    <button>{{ 'nav.search.btn' | trans }}</button>
                </div>
            </div>
        </nav>